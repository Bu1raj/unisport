import 'package:flutter/material.dart';

class TournamentMainScreen extends StatelessWidget {
  const TournamentMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.emoji_events),
    );
  }
}